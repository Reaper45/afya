export interface ITheme {
  colors: {
    primary: string;
    dark: string;
    light: string;
    secondary: string;
    icon: string;
  };
}

export const theme: ITheme = {
  colors: {
    primary: "#5A67D8",
    dark: "#3C366B",
    light: "#A3BFFA",
    secondary: "#222222",
    icon: "A0AEC0"
  }
};
